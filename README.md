# TakeMeter: Stack Overflow Python Discourse Classifier

TakeMeter is a fine-tuned text classifier for evaluating discourse quality and function in the public **Stack Overflow `python` tag** community. The project classifies individual questions, answers, and comments into four labels that distinguish useful technical discussion from terse fixes and low-context reactions.

> Status: dataset generated with weak heuristic labels. Manual label review, model training, baseline evaluation, and final metrics still need to be completed before submission.

## Community choice

I chose the public **Stack Overflow `python` tag** community because it is active, public, text-heavy, and centered on technical learning. The discourse varies enough to support a meaningful classification task: some questions provide detailed debugging context, some answers teach concepts, some answers give only a direct fix, and some comments are mostly thanks, clarification requests, or low-context follow-ups.

These distinctions matter to participants because a beginner programming community depends on clear help-seeking and useful explanation. A correct answer that teaches the reason behind a bug has different community value than a one-line fix, and a well-formed debugging request is easier for volunteers to answer than a vague complaint.

## Label taxonomy

The model uses four mutually exclusive labels.

| Label | Definition |
| --- | --- |
| `diagnostic_request` | A post or comment asking for help with a Python problem while providing enough concrete information to diagnose it, such as code, an error message, expected behavior, observed behavior, or a specific technical question. |
| `explanatory_answer` | A response that teaches or explains a Python concept, debugging principle, or reasoning process, not merely the final answer. |
| `direct_fix` | A concise answer that gives a code snippet, command, API name, or direct correction with little or no explanation of why it works. |
| `low_context_reaction` | A post or comment that is mostly emotional reaction, social feedback, vague complaint, joke, thanks, meta-commentary, or a help request without enough technical context to diagnose. |

### Examples per label

#### `diagnostic_request`

1. "I'm trying to read a CSV with pandas, but `read_csv` treats the first row as data instead of headers. My file starts with `name,age`, and this is my code: `pd.read_csv('people.csv', header=None)`. Why is the header not being detected?"
2. "This recursive function returns `None` after the first call. I expected it to return the final list. What am I missing?"

#### `explanatory_answer`

1. "The issue is that lists are mutable, so using `[]` as a default argument means every call shares the same object. Use `None` as the default and create a new list inside the function."
2. "`range(10)` stops before 10 because Python ranges are half-open. This makes loops line up with zero-based indexing and length calculations."

#### `direct_fix`

1. "Use `json.loads(response.text)` instead of `json.load(response.text)`."
2. "Change `if x = 3:` to `if x == 3:`."

#### `low_context_reaction`

1. "Python is so confusing, I give up."
2. "Thanks, that fixed it!"

## Edge-case rules

The most important boundary is between `direct_fix` and `explanatory_answer`. If a response includes a reason, tradeoff, or concept that helps the learner generalize beyond the immediate fix, I label it `explanatory_answer`. If it only provides the changed line, API name, or command, I label it `direct_fix`.

For help requests, emotional language does not decide the label. A frustrated post with code and expected behavior is still a `diagnostic_request`; a vague "why doesn't this work?" post without diagnostic details is `low_context_reaction`.

## Data collection and annotation

The dataset was collected from public Stack Overflow content using the Stack Exchange public API.

- **Source:** Public Stack Overflow questions, answers, and comments associated with the `python` tag.
- **Collection script:** `scripts/collect_stackoverflow_python.py`.
- **Dataset file:** `data/labeled_examples.csv`.
- **Total examples:** 220.
- **Columns:** `text`, `label`, `notes`, `source_url`.
- **Labeling process:** The current CSV uses weak heuristic labels generated by the collection script. Questions are initially labeled `diagnostic_request`; answers are split between `explanatory_answer` and `direct_fix` using length and explanation-cue rules; short or reaction-like comments are labeled `low_context_reaction`. These labels must be manually reviewed and corrected before treating the dataset as final ground truth.

### Label distribution

| Label | Count | Percent |
| --- | ---: | ---: |
| `diagnostic_request` | 55 | 25% |
| `explanatory_answer` | 55 | 25% |
| `direct_fix` | 55 | 25% |
| `low_context_reaction` | 55 | 25% |
| **Total** | 220 | 100% |

### Difficult-to-label examples

TODO: Replace these planning examples with at least three real examples from the dataset.

| Text | Possible labels | Final label | Why |
| --- | --- | --- | --- |
| "Use a virtual environment." | `direct_fix`, `explanatory_answer` | `direct_fix` | It gives advice but no reason or teaching context. |
| "Can someone explain classes? I don't get them." | `diagnostic_request`, `low_context_reaction` | `low_context_reaction` | The request is too broad to diagnose or answer concretely. |
| "Your indentation is wrong; Python uses indentation to define blocks." | `direct_fix`, `explanatory_answer` | `explanatory_answer` | It gives the underlying Python rule, not only the correction. |

## Fine-tuning approach

TODO: Complete after running the Colab notebook.

- **Base model:** `distilbert-base-uncased`.
- **Training environment:** Google Colab with T4 GPU.
- **Training data split:** The starter notebook creates a stratified 70% train, 15% validation, and 15% test split.
- **Initial hyperparameters:** 3 epochs, learning rate `2e-5`, train batch size 16, eval batch size 32, max token length 256.

I chose the default 3-epoch setup initially because the dataset is small. More epochs might improve training accuracy but could also overfit to only 200 examples.

## Zero-shot baseline

TODO: Complete after running Section 5 of the notebook with Groq.

- **Baseline model:** Groq `llama-3.3-70b-versatile`.
- **Test set:** Same locked test split used for the fine-tuned model.
- **Output format:** The prompt instructed the model to output only one label name.

### Baseline prompt

TODO: Paste the exact prompt used here.

```text
Classify the following Stack Overflow Python question, answer, or comment into exactly one label.

Labels:
- diagnostic_request: A post or comment asking for help with a Python problem while providing enough concrete information to diagnose it, such as code, an error message, expected behavior, observed behavior, or a specific technical question.
- explanatory_answer: A response that teaches or explains a Python concept, debugging principle, or reasoning process, not merely the final answer.
- direct_fix: A concise answer that gives a code snippet, command, API name, or direct correction with little or no explanation of why it works.
- low_context_reaction: A post or comment that is mostly emotional reaction, social feedback, vague complaint, joke, thanks, meta-commentary, or a help request without enough technical context to diagnose.

Output only one of these label names:
diagnostic_request
explanatory_answer
direct_fix
low_context_reaction

Text: {text}
```

## Evaluation report

TODO: Complete after running baseline and fine-tuned evaluation.

### Overall comparison

| Model | Accuracy |
| --- | ---: |
| Zero-shot Groq baseline | TODO |
| Fine-tuned DistilBERT | TODO |

### Baseline per-class metrics

| Label | Precision | Recall | F1 | Support |
| --- | ---: | ---: | ---: | ---: |
| `diagnostic_request` | TODO | TODO | TODO | TODO |
| `explanatory_answer` | TODO | TODO | TODO | TODO |
| `direct_fix` | TODO | TODO | TODO | TODO |
| `low_context_reaction` | TODO | TODO | TODO | TODO |

### Fine-tuned model per-class metrics

| Label | Precision | Recall | F1 | Support |
| --- | ---: | ---: | ---: | ---: |
| `diagnostic_request` | TODO | TODO | TODO | TODO |
| `explanatory_answer` | TODO | TODO | TODO | TODO |
| `direct_fix` | TODO | TODO | TODO | TODO |
| `low_context_reaction` | TODO | TODO | TODO | TODO |

### Fine-tuned confusion matrix

Rows are true labels. Columns are predicted labels.

| True \ Predicted | `diagnostic_request` | `explanatory_answer` | `direct_fix` | `low_context_reaction` |
| --- | ---: | ---: | ---: | ---: |
| `diagnostic_request` | TODO | TODO | TODO | TODO |
| `explanatory_answer` | TODO | TODO | TODO | TODO |
| `direct_fix` | TODO | TODO | TODO | TODO |
| `low_context_reaction` | TODO | TODO | TODO | TODO |

A generated image version should also be committed as `confusion_matrix.png` after running the notebook.

### Wrong predictions and analysis

TODO: Add at least three specific fine-tuned model errors.

| Text | True label | Predicted label | Analysis |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO |
| TODO | TODO | TODO | TODO |
| TODO | TODO | TODO | TODO |

Questions to answer in this analysis:

- Which labels were confused?
- Was the error caused by short text, missing thread context, ambiguous wording, sarcasm, or a weak label boundary?
- Is this mainly a labeling issue, a data distribution issue, or a model limitation?
- What data or label change would likely fix it?

### Sample classifications

TODO: Add 3–5 examples run through the fine-tuned model.

| Text | Predicted label | Confidence | Notes |
| --- | --- | ---: | --- |
| TODO | TODO | TODO | TODO |
| TODO | TODO | TODO | TODO |
| TODO | TODO | TODO | TODO |

At least one correctly predicted example should include a sentence explaining why the prediction is reasonable.

## Reflection: intended vs. learned behavior

TODO: Complete after reviewing errors.

The intended behavior is for the model to learn discourse function and usefulness: whether text asks a diagnosable question, explains a concept, gives a terse fix, or contributes little technical context. After evaluation, describe what the model actually seemed to learn. For example, it may overfit to surface cues such as question marks, code formatting, short length, or words like "thanks" rather than the deeper distinction between explanation and direct fix.

## Spec reflection

TODO: Complete at the end.

- **How the spec helped:** TODO. For example, the planning spec forced the label boundaries to be defined before annotation, especially the distinction between `direct_fix` and `explanatory_answer`.
- **How implementation diverged:** TODO. For example, if one label was underrepresented or the edge-case rules changed after reading real posts, explain what changed and why.

## AI usage

TODO: Complete before submission with at least two specific instances.

Example structure:

1. **Dataset collection and weak labeling:** I used an AI coding assistant to create `scripts/collect_stackoverflow_python.py`, which calls the Stack Exchange public API and assigns initial labels using transparent heuristics. I still need to manually review the generated labels before final submission.
2. **Label stress-testing:** I asked an AI tool to generate borderline examples between `direct_fix` and `explanatory_answer`. It produced examples where a one-line answer included a tiny explanation. I revised the rule so any generalizable reasoning counts as `explanatory_answer`.
3. **Failure analysis:** After training, I will give the model's wrong predictions to an AI tool and ask for error patterns. I will verify those patterns by rereading the examples and checking the confusion matrix.

No LLM labels should be treated as ground truth without manual review.

## Repository contents

| Path | Purpose |
| --- | --- |
| `planning.md` | Project design, label taxonomy, data plan, evaluation plan, and AI tool plan. |
| `Copy_of_ai201_project3_takemeter_starter_clean.ipynb` | Starter Colab notebook. |
| `copy_of_ai201_project3_takemeter_starter_clean.py` | Python export of the starter notebook. |
| `data/labeled_examples.csv` | Weak-labeled dataset collected from public Stack Overflow Python content. |
| `evaluation_results.json` | TODO: exported evaluation results from Colab. |
| `confusion_matrix.png` | TODO: exported fine-tuned model confusion matrix from Colab. |

## How to reproduce

TODO: Update after completing the dataset and notebook run.

1. Open `Copy_of_ai201_project3_takemeter_starter_clean.ipynb` in Google Colab.
2. Set runtime to T4 GPU.
3. Define the label map:

```python
LABEL_MAP = {
    "diagnostic_request": 0,
    "explanatory_answer": 1,
    "direct_fix": 2,
    "low_context_reaction": 3,
}
```

4. Upload the labeled CSV.
5. Run Sections 1 and 2 to split/tokenize the data.
6. Run Section 5 for the Groq zero-shot baseline.
7. Run Sections 3 and 4 to fine-tune and evaluate DistilBERT.
8. Run Section 6 to export `evaluation_results.json` and `confusion_matrix.png`.
